{% extends 'base.html.twig' %}

{% block title %}Détail {{ data.0.name.common }}{% endblock %}

{% block body %}

    <div class="card m-3" style="width: 18rem;">
        <img class="border border-dark" src="{{ data.0.flags.png }}">
        <div class="card-body text-center">
            <h3>{{ data.0.name.common }}</h3>
            <p class="fw-light">{{ data.0.capital.0 }}</p>
            <p>Langue nationale : {{ data.0.languages | first }}</p>
            <p>{{ data.0.region }}</p>
            <p>Monnaie : {{ data.0.currencies | first | first}}</p>
            <p><span class="badge bg-danger">{{ data.0.population }} habitants</span></p>
        </div>
    </div>

{% endblock %}